pipeline:
  benchmarks_dir: /benchmarks
  fastq_ext: .fastq.gz
  log_dir: /logs
  pipeline_input: null
  results: /results
  sample_groups:
    condition: null
    control: null
  sample_type: single-end
  sample_fastq:
    condition: null
    control: null
  threads: 6
  work_dir: null
qc:
  output_dir: /trimmed
counts:
  input_dir: /mapped
  output_dir: /counts
metrics:
  input_dir: /mapped
  output_dir: /metrics
alignment:
  input_dir: /trimmed
  output_dir: /mapped
  ref_genome: /reference_genome
aggregate:
  output_dir: /aggregate
diffexp:
  input_dir: /counts
  output_dir: /diffexp
